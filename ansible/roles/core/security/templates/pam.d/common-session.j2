# {{ ansible_managed }}
# SummitEthic PAM configuration for sessions

# Session settings
session     required        pam_limits.so
session     required        pam_unix.so
session     optional        pam_umask.so
session     optional        pam_systemd.so
session     optional        pam_permit.so