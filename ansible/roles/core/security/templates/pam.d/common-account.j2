# {{ ansible_managed }}
# SummitEthic PAM configuration for account management

# Account settings
account     required        pam_unix.so
account     required        pam_faillock.so
account     required        pam_time.so
account     optional        pam_permit.so